# 這次用的架構是什麼、為什麼不是「把網頁丟上主機」

## 傳統做法 vs 這次的做法

如果只是「單純把網頁放上 web server」，通常是：租一台主機（VPS）或買虛擬主機空間，把 HTML 檔案傳上去，網址就能打開了。但這樣只解決了「網頁打得開」這一件事——資料要存哪裡、誰登入誰的帳號、怎麼跨裝置同步，這些統統要自己另外寫程式、自己架資料庫、自己維護。

這次的做法把整個系統拆成三塊，各自交給一個現成的免費服務顧，不用自己維護伺服器：

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│  GitHub Pages    │     │     Firebase       │     │  PWA 設定        │
│  （前台：網頁本身） │     │  （後台：帳號+資料庫） │     │  （外觀：像 App）  │
└─────────────────┘     └──────────────────┘     └─────────────────┘
   放 index.html            存您的筆記內容             manifest.json
   免費、自動 HTTPS          帳號登入、即時同步            + service worker
   git push 就更新           不用自己寫後端 API           「加入主畫面」變 App
```

這種「前台放靜態網頁、後台交給現成雲端服務」的組合，業界常稱作 **Serverless**（無伺服器架構）或 **JAMstack**——意思是你不用自己管一台伺服器 24 小時跑著，兩塊拼圖各自都有別人幫你維護好、按用量付費（這個規模完全在免費額度內）。

---

## 拼圖一：GitHub Pages（前台）

**解決的問題**：網頁要放在哪裡，手機才連得到。

- 本機的 `index.html` 是您電腦裡的一個檔案，只有這台電腦打得開
- `git push` 把它送到 GitHub 上一個叫 `mandala-notes` 的倉庫（repo）
- GitHub Pages 是 GitHub 附贈的功能：只要 repo 是公開的，它會自動把裡面的網頁檔案架成一個網站，網址是 `https://abbestben.github.io/mandala-notes/`
- 好處：免費、自動幫您處理 HTTPS 加密、有 CDN（全球加速），而且**每次 `git push` 它會自動重新部署**，不用手動上傳

跟傳統主機的差異：傳統主機通常要自己申請 SSL 憑證、自己設定網頁伺服器軟體（Apache/Nginx）；GitHub Pages 這些都內建好了，代價是它只能放「靜態」網頁（沒有自己的後端程式可以跑），這也是為什麼需要拼圖二。

## 拼圖二：Firebase（後台）

**解決的問題**：單純的靜態網頁沒有「記憶」——資料只能存在瀏覽器自己的 localStorage 裡，換一台裝置、換一個瀏覽器就看不到了。要跨裝置同步，需要一個大家都連得到的資料庫，而且要知道「這是哪個人的資料」（帳號系統）。

Firebase 是 Google 提供的 **BaaS（Backend as a Service，後端即服務）**：不用自己寫伺服器程式、不用自己架資料庫，直接在網頁的 JavaScript 裡呼叫 Firebase 的 SDK 就能用兩個功能：

- **Authentication**：處理「登入是誰」，這次用 Email/密碼方式，讓同一個人在 Mac 和手機用同一組帳密登入時，兩邊認得出是同一個使用者
- **Firestore**：一個雲端資料庫，您的筆記內容存在這裡，登入後任何一台裝置改了資料，程式碼裡的 `onSnapshot` 會即時通知其他裝置「資料變了」，畫面就自動更新——這就是跨裝置同步的原理

資料流程大致是：

```
您在手機打字
   → 先存進手機瀏覽器的 localStorage（不用等網路，馬上存好）
   → 0.9 秒後背景推一份到 Firestore（雲端）
   → Mac 那邊的瀏覽器透過 onSnapshot 監聽，收到通知
   → Mac 自動把新內容更新到畫面上
```

如果沒登入，就只有第一步，資料乖乖留在那台裝置本機，不會流到任何地方。

## 拼圖三：PWA（外觀）

**解決的問題**：網頁在手機瀏覽器打開，看起來就是個網頁，有網址列、跟其他分頁擠在一起，不像個「App」。

PWA（Progressive Web App）是一套網頁規格，讓瀏覽器知道「這個網站可以被當作 App 安裝」：

- `manifest.json`：告訴手機這個網站的名字、圖示、開啟時要不要顯示網址列（我們設成 `standalone`，也就是不顯示，看起來就跟原生 App 一樣）
- `sw.js`（Service Worker）：一段在背景常駐的程式，可以攔截網路請求、快取檔案，讓 App 在沒有網路時也能把畫面殼子打開（離線能力的基礎）

「加入主畫面」之所以會出現一個漂亮的圖示、開啟後沒有網址列，就是因為這兩個檔案在起作用。

---

## 這套做法的優點與限制

**優點**
- 全部免費（GitHub Pages 免費、Firebase 有夠大的免費額度）
- 不用自己管伺服器、不用擔心當機重開機
- 改完程式碼只要 `git push`，一兩分鐘全世界（包含您的手機）就看到新版本
- 資料同步不用自己寫後端 API，Firebase SDK 都包好了

**限制**
- Firebase 免費額度雖然對個人使用綽綽有餘，但如果某天用量暴增（例如被很多人用），會需要開始付費
- GitHub Pages 要求 repo 是公開的，程式碼任何人都看得到（但您的筆記內容不會，這點前面聊過）
- 這種「前台 + BaaS」的組合，不適合需要複雜後端邏輯（例如金流、大量運算）的專案——不過對筆記工具這種需求，完全夠用

---

## 以後想做類似的東西，可以怎麼複製這套模式

1. 寫一個純前端網頁（HTML/CSS/JS，或用框架都行）
2. 建一個 GitHub repo，`git push` 上去，開啟 GitHub Pages
3. 如果需要帳號、雲端資料，就申請一個 Firebase 專案，接 Authentication + Firestore
4. 如果想讓手機使用者能「安裝」成 App 的感覺，加一份 `manifest.json` 和最簡單的 `sw.js`

這四步就是這次從頭到尾實際做過的事，之後不管做什麼新工具，都可以照這個套路走一遍。
